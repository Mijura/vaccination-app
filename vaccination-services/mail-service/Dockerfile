FROM openjdk:8-jdk-alpine
VOLUME /tmp
COPY ./target/*.jar app.jar

ARG CONFIG_URL
ARG DISCOVERY_URL
ARG MAIL_PORT

CMD java -jar /app.jar -DCONFIG_URL=${CONFIG_URL} -DDISCOVERY_URL=${DISCOVERY_URL} -DMAIL_PORT=${MAIL_PORT}